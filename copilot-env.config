# Gateway-float version bounds, read by src/gateway_float.ts (+ start.ts).
# Format: KEY=value. An empty MAX means "no ceiling"; "null" is also accepted.
GATEWAY_MIN_VERSION=1.11.0
GATEWAY_MAX_VERSION=
